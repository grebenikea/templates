#include <iostream>
#include <string>
#include <vector>
#include <math.h>

vector<pair<int, int>> factorization(int n)
{
	vector<pair<int, int>> result;

	for (int c1 = 2; c1 <= v.size(), ++c1)
	{
		int k = 0;
		if (n % c1 == 0)
		{
			for (int c2 = 2; c2 < c1; ++c2)
			{
				k = 0;
				if (c1 % c2 == 0)
					k++;
			}
		}
	}
}

//я написала алгоритм для вычисления простых делителей
//а как довести это до конца, не придумала